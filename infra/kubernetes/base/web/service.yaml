apiVersion: v1
kind: Service
metadata:
  name: reliabilityhub-web
  namespace: reliabilityhub-system
  labels:
    app: reliabilityhub-web
spec:
  selector:
    app: reliabilityhub-web
  ports:
    - name: http
      port: 3000
      targetPort: http
  type: ClusterIP
